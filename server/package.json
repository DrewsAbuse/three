{
  "name": "test-one",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsc --watch & node --watch dist/server.js",
    "install:build": "npm install & tsc --build tsconfig.json",
    "start": "npm run build & node dist/main.js",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "prettier": "@shelf/prettier-config",
  "devDependencies": {
    "@shelf/eslint-config": "3.10.0",
    "@shelf/prettier-config": "1.0.0",
    "@types/node": "20.8.9",
    "esbuild": "0.19.5",
    "eslint": "8.36.0",
    "prettier": "3.0.3",
    "typescript": "5.2.2"
  }
}
